# android-ebook

安卓小说阅读器应用，与 lib_common（android-practice）联动开发的项目。本语境描述内容的获取、存储、阅读与社交互动。

## 内容获取

**书源（BookSource）**:
可配置的第三方内容来源，由规则描述其接口行为。
清单持久化在库中，**全部由用户自行导入**（应用不随包携带任何书源），各自可启用/禁用/设默认/删除；
**每一本书绑定一个书源**，解析按该书的「书源归属标记」取源，不再全局共用一个源
（只有书城与「新加的书绑哪个源」例外，那两处按默认书源走）。
_Avoid_: 书源网站、来源

**书源归属标记（tag）**:
一本书来自哪个书源的标识，取值为该书源的 URL；书架、书籍详情、章节索引、下载任务四张表的行都带它。
它是「按哪套规则解析这本书」的唯一依据，因此**禁用一个书源不切断已有归属**（禁用的源仍能被自己
那本书解析到），而**删除一个书源会让归属悬空**——那本书解析不出内容，只报「书源已失效」。
本地书的归属是固定值 `loc_book`，不指向任何书源。
_Avoid_: 来源标签、书源 ID

**多书源共存（Multi-Source Coexistence）**:
书架上来自不同书源的书同时存在、各自按自己的归属解析的状态（见 ADR-0016）——一本能读不代表另一本也能读。
_Avoid_: 书源池、多源同步

**默认书源（Default BookSource）**:
书城浏览所用的那一个书源，永不为禁用态。
它是「新加入的书绑哪个源」的答案，**不再是「已有内容按哪个源解析」的答案**——后者归书源归属标记管。
用户在书城顶部的书源切换器上换的就是它（换源即持久化，重启后仍是上次选中的那个）。
启用中的源被全部禁用时它为空，书城与搜索同时进入「无源可用」。
它的事实源是**库里那条书源行**，外加一份「用户选的是哪一条」的持久记号（SP 只承载这份记号，
不承载源本身）。读库是异步的，因此书城首帧有一段**还不知道**的状态——整片留空（既不画引导语、
也不画半截切换器），读库落定后才落进「有源 / 无源 / 源在但解析不出来」。这段空白**不是**「没有源」，
拿后者兜底会让有源用户的每次冷启动先闪一帧误导文案。
重新导入同 URL 的规则即让它整体换掉——书城与所有解析路径同时跟着新规则走，
不存在「库里已是新规则、解析仍在用旧规则」的中间态。
「无源可用」与「当前源已失效」是两种空态、两句话：前者一条源都没启用（去启用或导入），
后者源还在、但那一条已解析不出来（换一个源或重导这条源），后者不得被说成前者。
_Avoid_: 当前书源、主书源

**聚合搜索（Aggregate Search）**:
一次关键词搜索同时发给全部启用中的书源（两种出身都算候选），结果**边收边并入一份列表**，而不是等最慢的源回齐。
两条不看实现猜不到的领域规则：
- 每条结果自带书源归属（`tag` = 所属源 URL、`origin` = 所属源名），加书架与后续解析都按**这本书自己的**
  源走，与默认源无关；
- 同名同作者但来自两个源的两本书**刻意都留着**，那是「阅读中换源」的候选，不按书名合并。
单个源失败只影响它自己（其余源的结果照常给出），全部源都失败才算这次搜索失败。
_Avoid_: 全网搜索、多源搜索

**阅读中换源（Source Switch）**:
把手上这本书换成另一个书源的同一本书并接着往下读——换的是来源载体，**章级**进度与评论归属都跟着书走；
**页级进度不跨源**（同一章在两个站的正文长度与分页规则都不同，旧页码在新章里既可能越过文末也可能落回中段，
从第一页开始是唯一不会读错内容的选择）。
换完书架上仍是那一本书：旧源那条随之消失，不是多出让出一条。
两条不成换源的情形：目标那条**本来就在书架上**（那是另一条已有条目，换过去等于把它覆盖掉——
不叫换源，用户要的是去读那一本）；换源不迁移旧源的在队下载任务（那些地址属于旧站，
按新源重跑就是拿旧 URL 打新站），旧那批任务随旧条目一起作废。
_Avoid_: 切换书源（不特指时会被读成「换默认书源」，那是书城顶部那颗胶囊做的事）

**用户自备书源（User-Provided BookSource）**:
书源清单里每一行的唯一来历——应用**不随包携带任何书源**（书源是指向第三方内容站点的访问配置，
随包分发等于由应用内置一份这样的配置），用户从自己信任的来源导入（本机文件，原生规则与脚本两种
出身都算）。**不存在「内置源」这回事**，因此也没有「哪一行受保护」的分支：任何一条都可启用/禁用、
可设为默认、可删除。
_Avoid_: 内置源、默认源（「默认书源」是另一个概念：书城当前用的那一个）

**书源规则（BookSourceRule）**:
描述单个书源的解析规则（搜索/章节索引/正文的 URL 与选择器、字符集、请求方法）。
_Avoid_: 规则

**出身（Source Format）**:
一条书源**按哪套语义被解释**的维度：原生规则 / 脚本。两种出身在同一份清单里并存，取源解析时按它路由到
各自的解释器；读写必须取同一个值——漂移不会报错，只会让脚本出身的源按原生规则去解，拉回的是不相干的内容。
它说的是这条源**怎么说**，与「清单里的哪一条」无关——来历只有一种（用户自备）。
_Avoid_: 书源类型（用时点明是「出身」：原生还是脚本）

**原生规则书源（Native-rule BookSource）**:
两种出身之一：以本仓自己的 [书源规则（BookSourceRule）](#内容获取) 表达的书源，
纯声明式——URL 模板、选择器与正则清洗，规则里没有可执行代码。
与脚本书源在库中并存，按出身路由到各自的解释器。
_Avoid_: 普通书源（与「脚本」对举时点明是「原生」）

**脚本书源（Script-based BookSource）**:
两种出身之一：以社区通用 JSON 格式表达、规则内嵌可执行脚本（JavaScript）的书源。
入库即存原始 JSON、不做翻译转换，解析按该格式自己的语义求值；其中的脚本一律在
零权限的隔离进程中执行（超时、内存上限、崩溃自动重启），**永不以应用权限进入主进程**。
格式不支持的能力（登录、浏览器自动化等）导入时逐条明示，不静默产出坏源。
全仓不使用该生态的项目名指代这个格式。
_Avoid_: 外部源、第三方书源、翻译源

**规则串（Rule String）**:
脚本书源格式里「从一页里取一个值」的那条表达式字符串（如 `@css:.bookbox@tag.a.0@text##全文`），
求值的输入单元：一个模式标志开头，中间由组合符串起若干段，段上可带索引与替换段。
它是**源作者写的文本**，不是本仓的 [书源规则（BookSourceRule）](#内容获取)——后者是整本源的配置对象，
两者不在一个层次。
_Avoid_: 规则（裸用会同时撞上「书源规则」与「规则串」）、脚本（规则串本身不可执行，只有其 JS 段才交执行器）

**模式标志（Rule Mode）**:
规则串开头声明「这一串按哪种语法解」的前缀（链式默认、CSS、JSONPath、正则、JS、变量存取…）。
**判定次序本身就是语义**：先匹配者胜，且标志必须在串被切开之前判定——标志字符同时也是切分字符，
先切再判会把标志切成一个空段。识别出来但本仓不实现的标志一律如实报（按不支持拒绝，或计入导入报告），
**绝不静默回落成默认模式**：那等于拿链式规则去解正则，解出来的内容不相干却不报任何错。
_Avoid_: 前缀、语法标记

**脚本执行器（Script Executor）**:
承载不可信脚本段的零权限隔离进程，内含自集成的 JS 引擎与自有桥接层。主进程只递进去「一段脚本加当次绑定」、
拿回来「一个结果或一个失败档案」，**永不加载引擎**；执行器崩溃不传染业务进程，只让该源本轮请求按失败处置。
「沙箱」/`sandbox` 是同一物的机制名（取自 Android 的隔离进程措辞），代码与文档里两名混用、所指不变。
_Avoid_: 用「沙箱进程」「JS 引擎」指代整体（引擎只是它内部的一块）——
但这两词的约束针对的是行文用词，不针对标识符，不必为此改名

**受限代理（Restricted Proxy）**:
**脚本自己发起的那族外呼**的唯一出口——请求由主进程代发：仅 http(s)、拒绝私网、可解析 host 白名单按源规则原文
导出、大小/超时/频度上限齐备，**永不附加用户凭证**。脚本自身没有开 socket 的能力，网络只存在于执行器之外。
守门**尚未覆盖取文链路**：分类页与正文那些 URL（含 URL 选项里的脚本改写出的地址）由取文层按原路径发出，
同样不带凭证，但不过白名单与私网判定。因此「脚本出网一律受守门约束」不等于「这本书源发出的每个请求都受守门
约束」，两处口径不可合并陈述；把取文并进同一条守门是登记在 ADR-0028 遗留里的后续项。
_Avoid_: 网络桥、代理客户端；把「所有出网请求都过守门」当成既成事实

**分类入口（Explore Entry）**:
书城页「书籍类型」胶囊上一个分类一个入口的那份清单：分类名 + 该源解析它所需的地址。
两种出身给同一形态——原生规则书源给配置里的分类清单，脚本书源给发现条目，
后者的地址承载的可能是**规则串**而非一个现成的 URL。
一条领域规则：入口里的地址**只对生成它的那个书源有意义**，换源之后继续用旧入口，
得到的是一份不相干的书目且毫无报错——所以分类入口必须与当时的默认源一起取、一起换。
_Avoid_: 分类标签（那个词指**一本书自己的**分类标签，不是分类页清单）

**软 404（Soft 404）**:
第三方站点对越界页返回 **HTTP 200 + 首页那份内容**，而不是 404 或空页。
由此「这一页是空的」与「状态码是 2xx」都不足以判「到底」：列表与目录一律按条目主键去重，
**这一页没带来新条目**才算到底。
_Avoid_: 假 404、伪 404

**分类浏览（Kind Browse）**:
从书城进入单个分类的全量分页列表（「加载更多」逐页取全量）。
与之相对，书城首屏各分类只展示**首页样张**——每分类第 1 页、整体缓存，仅作入口；
全量浏览与翻页属于分类浏览。
_Avoid_: 书库页、发现页

**书城（Find）**:
按默认书源浏览与搜索书籍的入口（换源靠它顶部那颗切换胶囊）。
_Avoid_: 发现、书库浏览

## 内容存储

**书架（Bookshelf）**:
用户主动收藏的书籍集合，是阅读与管理的起点。
_Avoid_: 书籍列表、我的书籍

**搜索历史（SearchHistory）**:
用户在本地的搜索词记录（区分搜索类型，如书籍搜索）。搜索页的历史面板整体展示并供点选快捷搜索，面板不做子串过滤；清除操作清空该类型全部历史。
_Avoid_: 历史记录、搜索记录

**书籍（Book）**:
书架上的一本书，关联书源、章节与阅读进度。

**章节（Chapter）**:
书籍内容的基本组织单位。

**章节缓存（Chapter Cache）**:
正文的内存缓存层，用于翻页时避免重复读盘。本地书与网络书的正文均落盘为章文件（见「内容仓库」），内存缓存是它们共享的读取加速层。
_Avoid_: 缓存

**内容仓库（Content Store）**:
应用私有目录里为一本书保留的全部章文件，**一本书一个子目录**，目录名即该书的主键（本地书是导入文件的
内容 md5，网络书是它在书源那边的书籍根地址）。本地书与网络书正文的**统一**落盘位置。
_Avoid_: 缓存目录、书目录

**应用缓存（App Cache）**:
可再生或可重新下载的那部分占用——图片磁盘缓存、书库磁盘缓存（书城各分类首屏，过期或清理后重抓即得）、
裁剪中间文件等。它是「可以安全清理」的范畴；
内容仓库里的书籍正文**不属于**应用缓存，清掉它就是删用户的藏书。设置页「缓存管理」的口径以此为准。
_Avoid_: 缓存（裸用，会与「章节缓存」相混）、临时文件（指整类时）

**章节索引（Chapter Index）**:
一本书"有哪些章、每章在哪"的列表；本地书条目指向章文件路径，网络书条目指向章节 URL。
_Avoid_: 目录（与文件系统目录混淆）

**内容定位符（content_ref）**:
章节索引行上「这一章的正文在哪」那个值：本地书是章文件的相对路径，网络书是该站章节 URL。
它同时是这一行的主键——一章一定位符，所以**同一部书在两个源上的同一章是两个不同的行，谁也不覆盖谁**；
目录翻页判「到底」也按它跨页去重（越界页会以软 404 重复返回上一批章，见上「软 404」）。
_Avoid_: 章节地址、章路径（各自只说中一半出身）

**作品（Work）**:
跨来源的同一部书。只以客户端派生的不透明键 `comment_key` 存在，**没有**作品表也没有作品 ID。
_Avoid_: 书目、书籍聚合

**评论标识键（comment_key）**:
`ck1:` + 归一化书名与作者的 sha256，评论桶键。后端只按其过滤、不解释、不校验。
_Avoid_: 书 ID、work_key

**判重（导入时点）**:
待导入文件的 `comment_key` 等于书架某条目**当前主键**这一事实。
判的是"是不是同一作品"，不是"书名是否相同"——后者会把同名不同作者的两本书误判成一本。
_Avoid_: 同名检测、重复书名

**判重处置（导入时点的四种选择）**:
判重命中时用户可选的四种处置，破坏性一律不占主按钮位：
- **继续添加**——两个条目共存，非破坏的默认项，也是「共存」这一形态的正常入口；
- **智能合并**——把新文件多出的**尾部章节**补进旧条目、新条目退场，旧条目的阅读进度保留；
  只在命中项里有本地书时可用（补章的载体是本机章文件，网络书的正文在书源那边）；
- **覆盖**——导入新条目并删掉**全部**命中项，被删条目的阅读进度不保留；
- **取消**——这个文件不导入，批次里的其余文件照常往下走。
顺序一律**先导入新条目、后处置旧条目**：三种处置都要求新条目已是一份完整的书，
而「先删旧再导新」一旦导入失败就是两头空。
_Avoid_: 合并（不特指时会被读成覆盖）、去重（那是列表分页的口径，与这里无关）

**共存**:
同一作品在书架上保留多个来源条目（同键，各自一行主键），读评论取并集因而天然共享。
这是本模型支持的正常形态，不是待清理的脏数据。
_Avoid_: 把重复条目一律当作缺陷

## 阅读

**外观主题模式（Theme Mode）**:
用户对**应用整体**明暗的选择，三态：浅色 / 深色 / 跟随系统，默认跟随系统；改它即刻作用于全 App 的界面配色。
与下面的**阅读背景主题**是两个独立概念：阅读器豁免于本选择（整片固定浅色），设置页改了它也不会改变
阅读界面的背景。
_Avoid_: 主题（裸用会与「阅读背景主题」相混，指代时点明「外观主题」）

**阅读背景主题（Reading Background Theme）**:
阅读界面由用户主动选择的背景样式，四档定名（字体面板色卡上按此顺序展示）：素白、米黄、护眼、夜间。
与系统深色模式是两个独立概念——阅读界面不跟随系统深色，豁免于应用的深色适配。
_Avoid_: 深色模式（用于指阅读背景时）、夜间模式

## 社交

**评论（Comment）**:
用户对章节或书籍发表的评论，关联用户名与时间。

**评论时间（Comment Time）**:
评论的发表时刻。唯一来源是服务端下发的墙钟文本（Asia/Shanghai，精确到秒）：排序按秒分先后，
展示统一裁到分。**不按设备时区换算**——服务端给的就是它记录的那个时刻的字面写法，
换算会把「16:04」显示成「08:04」，那是另一个时刻。
_Avoid_: 时间戳（指展示串时）、创建时间

## 认证

**账号（Account）**:
邮箱（登录主标识，唯一）与用户名（展示用标识，可重复、注册时自动生成、可后改）的组合。
注册为三步：邮箱发码 → 验证码+密码建号（即激活，不发 token）→ 用户主动登录（见 ADR-0009）。
手机号不作为账号标识——项目无短信通道，不做手机号语义的校验与文案暗示。
昵称与头像同属展示用标识（用户可后改，缺省时展示默认头像），与登录标识无关：改昵称或换头像不改变这是哪个账号。
_Avoid_: 用户（指账号标识时）、手机号

**业务码（ErrorCode）**:
API 响应信封中承载业务成败的五位码（"00000" 成功，A=用户端错误/B=系统异常/C=第三方异常）。
与 HTTP 状态码是两个独立概念：HTTP 只表示业务层是否给出响应（恒 200），业务成败一律由业务码表达（见 ADR-0007）。
_Avoid_: 错误码（指 HTTP 状态码时）、状态码

**用户会话（UserSession）**:
身份（含昵称、头像等展示信息）与认证凭证（双 token）的组合。
**清会话即二者全部失效**：身份不只存在持久化副本里，还有进程内的内存态镜像，清除必须覆盖到每一处。
注意「轮换凭证」与「建立会话」是两个独立操作：登录/注册用 saveSession（身份+token），
静默刷新只用 rotateCredentials（仅换 token，绝不改写身份，见 ADR-0011）。
_Avoid_: 登录态（该词容易被用来专指凭证、或专指某一份持久化副本，从而掩盖「会话还包含身份」这件事）

**access token**:
短命认证凭证（JWT，2 小时有效，随请求携带）。**只驻内存**（lib_common 的 `TokenHolder`），不落盘——
冷启动为空，由首个请求 A0230 经静默刷新补上（见 ADR-0011）。
_Avoid_: TokenProvider（已废弃接口）

**refresh token**:
长命续期凭证（不透明串，服务端存哈希，30 天有效、刷新时轮换）。
仅用于刷新 access token，不随业务请求携带；客户端随会话持久化，密码不落盘（见 ADR-0008）。
**refresh 响应用户**：`POST /api/auth/refresh` 只返回新双 token、**不含用户资料**；
身份只在登录时下发并保存，刷新不再回填身份（见 ADR-0011）。
_Avoid_: 续期码

**静默刷新（Silent Refresh）**:
access token 过期（业务码 A0230）时，客户端用 refresh token 无感换新双 token 并重放原请求的机制。
刷新响应仅含新双 token（不回 user，身份保留）；刷新成功只轮换凭证（rotateCredentials）。
并发过期只刷一次（单飞）；刷新失败即会话「救不回来」，发「会话过期」事件统一处置——
**单响**：全局订阅方清会话+提示+跳登录，原调用方见过期标记只记日志、不重复弹 Toast（见 ADR-0010/0011）。
_Avoid_: 自动登录（旧密码重放方案，已废除）

## 发布与更新

**发布源（Release Source）**:
承载本 App 发行版清单的外部代码托管位置。当前两处：**主源**（优先请求）与**备用源**（主源不可达时降级请求），
二者指向同一仓库的同一批发布。顺序即优先级，不做可配置；「源」在这里指**发行版来源**，与内容域的
[书源（BookSource）](#内容获取)无关，两者不可混用同一个「书源/数据源」的说法。
_Avoid_: 更新服务器、镜像

**latest Release**:
发布源上「最新的一个正式发行版」。**预发布版本不参与**——它天然不是 latest，因此带尾缀的版本号
（见下「版本 tag」）出现在 latest 上时，语义是「同一版本号的后一轮发布」，不是「抢先试用版」。

**版本 tag**:
发布源上标记发行版的名称（如 `V1.3.0`、`V1.1.7alpha`），是版本比较的**唯一远端输入**；
本地侧的比较基准是装机应用的版本号（`versionName`，形如 `1.3.0`，不带前缀）。
两者形态不同：比较时前缀 `V`/`v` 一律忽略，展示时前缀只由文案决定一次（先归一化再插值）。
_Avoid_: 版本号（不加限定时指本地装机版本）

**新版角标（Update Badge）**:
设置页版本行上「有可更新版本」的提示。它是**派生态**而非存储态：由上次检查到的版本 tag 与当前装机
版本号现场比较得出，因此用户升级安装后角标即失效，不需要任何"清除角标"的动作。
判不出比较结果（tag 解析不出、本地版本号读不到）时不改变角标。
_Avoid_: 强制更新（本 App 无最低版本门禁概念）

**限频静默检查（Throttled Silent Check）**:
进入设置页时按限频窗口（距上次**成功**检查满 7 天）自动发起的一次版本检查：不弹窗、只更新角标，
失败则无声忽略且**不占用限频窗口**（失败的检查不算成功检查，时间戳不刷新）。
与认证域的 [静默刷新（Silent Refresh）](#认证) 是两件不同的事，**不复用同一个词**：
后者是续期凭证，前者是查版本。
_Avoid_: 自动检查更新（口语可用，代码与文档统一用本词）
