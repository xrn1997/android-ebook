# android-ebook

安卓小说阅读器应用，与 lib_common（android-practice）联动开发的项目。本语境描述内容的获取、存储、阅读与社交互动。

## 内容获取

**书源（BookSource）**:
可配置的第三方内容来源，由 JSON 规则描述其接口行为。
**当前为单书源架构**：书源来自随 APK 出货的内置清单（assets），运行时加载后全局共用一个默认书源。
多书源共存与运行时书源管理（导入/启用/禁用/删除、本地数据库持久化）是 ADR-0016 规划的**未来项，尚未实现**（见下相关术语）。
_Avoid_: 书源网站、来源

**书源规则（BookSourceRule）**:
描述单个书源的解析规则（搜索/目录/正文的 URL 与选择器、字符集、请求方法）。
_Avoid_: 规则

> 以下术语（默认书源、用户导入书源、多书源共存、书源归属标记、聚合搜索、阅读中换源）描述的是
> **ADR-0016 规划的多书源架构，当前尚未实现**，仅作为领域词汇先行登记；实现前代码仍是单书源现状。

**默认书源（Default BookSource）**:
用户在书源管理页显式选定的一个启用书源，作为**书城/书库浏览**的数据来源，
并在业务代码尚未按书找 parser 时作为兜底。默认书源**不影响**书架/阅读/下载——
这些路径按每本书自带的**书源归属标记**独立解析（见 ADR-0016）。
_Avoid_: 当前书源（歧义：可能指默认书源，也可能指某本书归属的书源）

**用户导入书源（User-Imported BookSource）**:
用户通过本地 JSON 文件导入的书源，可删除、可启用/禁用、可设为默认。
与**默认内置书源**（随 APK 出货、不可删只可禁用）相对。
_Avoid_: 自定义书源

**多书源共存（Multi-Source Coexistence）**:
书架上不同书籍可归属不同书源，各自按所属书源的规则解析（详情/目录/正文/下载），
互不干扰。每本书的归属由**书源归属标记**记录（见下）。
_Avoid_: 书源切换（歧义：可能被理解为「全局切换」，实际是「按书独立」）

**书源归属标记（Source Ownership Tag）**:
每本书/每章/每段缓存记录自身归属书源 URL 的字段（数据库实体的 `tag` 列）。
所有解析路径按此标记找对应书源的 parser，而非依赖全局默认书源（见 ADR-0016）。
_Avoid_: tag（歧义：其他上下文里 tag 可能指书籍分类标签）

**聚合搜索（Aggregated Search）**:
一次搜索请求并发打到所有启用书源，结果按 noteUrl 去重后聚合展示，
每条结果自带书源标签。某书源失败不阻塞其他书源；每书源独立分页游标。
_Avoid_: 全局搜索、多书源搜索

**阅读中换源（In-Reader Source Switch）**:
在阅读过程中，用当前书的**书名 + 作者**跨源搜索候选书，用户选中一本后重写书架记录：
换到新书源的同名书，按**章序号**映射阅读进度（越界回落到末章），旧书源的章节/正文缓存保留（用户可切回）。
换源为事务操作，失败回滚，书架记录不变（见 ADR-0016）。
_Avoid_: 换源（歧义：可能被理解为「换默认书源」）

**书城（Find）**:
按当前书源规则浏览与搜索书籍的入口。**当前为单书源，无顶部书源切换器**；书源切换胶囊与按源独立缓存是 ADR-0016 规划的未来项。
_Avoid_: 发现、书库浏览

## 内容存储

**书架（Bookshelf）**:
用户主动收藏的书籍集合，是阅读与管理的起点。
_Avoid_: 书籍列表、我的书籍

**搜索历史（SearchHistory）**:
用户在本地的搜索词记录（区分搜索类型，如书籍搜索）。搜索页的历史面板整体展示并供点选快捷搜索，面板不做子串过滤；清除操作清空该类型全部历史。
_Avoid_: 历史记录、搜索记录

**书籍（Book）**:
书架上的一本书，关联书源、章节与阅读进度。

**章节（Chapter）**:
书籍内容的基本组织单位。

**章节缓存（Chapter Cache）**:
本地持久化的章节内容，用于离线阅读与快速加载。
_Avoid_: 缓存

## 阅读

**阅读背景主题（Reading Background Theme）**:
阅读界面由用户主动选择的背景样式，四档定名（字体面板色卡上按此顺序展示）：素白、米黄、护眼、夜间。
与系统深色模式是两个独立概念——阅读界面不跟随系统深色，豁免于应用的深色适配。
_Avoid_: 深色模式（用于指阅读背景时）、夜间模式

## 社交

**评论（Comment）**:
用户对章节或书籍发表的评论，关联用户名与时间。

## 认证

**账号（Account）**:
邮箱（登录主标识，唯一）与用户名（展示用标识，可重复、注册时自动生成、可后改）的组合。
注册为三步：邮箱发码 → 验证码+密码建号（即激活，不发 token）→ 用户主动登录（见 ADR-0009）。
手机号不作为账号标识——项目无短信通道，不做手机号语义的校验与文案暗示。
_Avoid_: 用户（指账号标识时）、手机号

**业务码（ErrorCode）**:
API 响应信封中承载业务成败的五位码（"00000" 成功，A=用户端错误/B=系统异常/C=第三方异常）。
与 HTTP 状态码是两个独立概念：HTTP 只表示业务层是否给出响应（恒 200），业务成败一律由业务码表达（见 ADR-0007）。
_Avoid_: 错误码（指 HTTP 状态码时）、状态码

**用户会话（UserSession）**:
身份（含昵称、头像等展示信息）与认证凭证（双 token）的组合，由 UserSessionManager 管理。
**清会话即二者全部失效**：身份不只存在持久化副本里，还有进程内的内存态镜像，清除必须覆盖到每一处。
注意「轮换凭证」与「建立会话」是两个独立操作：登录/注册用 saveSession（身份+token），
静默刷新只用 rotateCredentials（仅换 token，绝不改写身份，见 ADR-0011）。
_Avoid_: 登录态（该词容易被用来专指凭证、或专指某一份持久化副本，从而掩盖「会话还包含身份」这件事）

**access token**:
短命认证凭证（JWT，2 小时有效，随请求携带）。**只驻内存**（lib_common 的 TokenHolder，
由 AuthInterceptor 附加到请求头），不落盘——冷启动为空，由首个请求 A0230 经静默刷新补上（见 ADR-0011）。
_Avoid_: TokenProvider（已废弃接口）

**refresh token**:
长命续期凭证（不透明串，服务端存哈希，30 天有效、刷新时轮换）。
仅用于刷新 access token，不随业务请求携带；客户端随会话持久化（替代旧方案中密码的键位），密码不落盘（见 ADR-0008）。
**refresh 响应用户**：`POST /api/auth/refresh` 只返回新双 token、**不含用户资料**；
身份只在登录时下发并保存，刷新不再回填身份（见 ADR-0011）。
_Avoid_: 续期码

**静默刷新（Silent Refresh）**:
access token 过期（业务码 A0230）时，客户端用 refresh token 无感换新双 token 并重放原请求的机制。
刷新响应仅含新双 token（不回 user，身份保留）；刷新成功只轮换凭证（rotateCredentials）。
并发过期只刷一次（单飞）；刷新失败即会话「救不回来」，发「会话过期」事件统一处置——
**单响**：全局订阅方清会话+提示+跳登录，原调用方见过期标记只记日志、不重复弹 Toast（见 ADR-0010/0011）。
_Avoid_: 自动登录（旧密码重放方案，已废除）

