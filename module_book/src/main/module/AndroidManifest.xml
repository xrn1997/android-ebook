<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        权限判据与取证见 ADR-0022。本文件（isModule=true 独立模式）必须与
        src/main/AndroidManifest.xml（集成模式）**逐条一致**——两份是替换关系，
        独立运行时只生效本文件，漏改就会出现「集成没声明、独立却还带着」的权限面分裂。
        此处保留的存储三项是**导入本地书**链路在用：
        MANAGE_EXTERNAL_STORAGE（全部文件访问，ImportBookActivity 直读文件树扫描
        epub/txt/pdf，见 ADR-0017）+ WRITE/READ_EXTERNAL_STORAGE（API ≤ 32 的回落路径）。
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Devices running Android 12L (API level 32) or lower  -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--    允许前台服务-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application
        android:name="debug.TestApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Common">
        <activity
            android:name="debug.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 独立模式模拟登录页：TestApplication 把 LOGIN_PATH 重定向到 TEST_LOGIN_PATH，
             该路由挂在本 Activity 上；不声明则跳转静默丢失（两份清单是替换关系，不是合并） -->
        <activity android:name="debug.LoginActivity" />
        <activity
            android:name="com.ebook.book.ReadBookActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
                <data android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
                <data android:scheme="file" />
            </intent-filter>
            <!-- EPUB：系统按 MIME 类型分发，content/file 两种 scheme 都需声明 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="application/epub+zip" />
                <data android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="application/epub+zip" />
                <data android:scheme="file" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ebook.book.ImportBookActivity"
            android:theme="@style/ToolbarTheme.BackBar" />
        <activity
            android:name=".BookCommentsActivity"
            android:label="本章评论"
            android:theme="@style/ToolbarTheme.BackBar" />
        <activity
            android:name="com.ebook.book.BookDetailActivity"
            android:theme="@style/ToolbarTheme.BackBar" />
        <activity
            android:name="com.ebook.book.DownloadManageActivity"
            android:label="下载管理" />
        <activity
            android:name=".EditBookMetaActivity"
            android:exported="false"
            android:label="编辑匹配信息"
            android:theme="@style/ToolbarTheme.BackBar" />

        <service
            android:name=".service.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>