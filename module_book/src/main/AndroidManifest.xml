<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        权限判据与取证见 ADR-0022。此处保留的存储三项是**导入本地书**链路在用：
        MANAGE_EXTERNAL_STORAGE（全部文件访问，ImportBookActivity 直读文件树扫描
        epub/txt/pdf，见 ADR-0017）+ WRITE/READ_EXTERNAL_STORAGE（API ≤ 32 的回落路径）。
        已删除：ACCESS_WIFI_STATE/CHANGE_WIFI_STATE（全仓无 WifiManager 用法）、
        READ_MEDIA_IMAGES/VIDEO/VISUAL_USER_SELECTED（相册选取走 PickVisualMedia；导入走的是
        全部文件访问而非 MediaStore 查询，故这三项无人消费）、
        GET_ACCOUNTS/READ_PROFILE/READ_CONTACTS（IDE 登录表单模板遗留，全仓无对应 API）。
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Devices running Android 12L (API level 32) or lower  -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--    允许前台服务-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Common">

        <service
            android:name=".service.DownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <activity
            android:name=".BookCommentsActivity"
            android:label="本章评论"
            android:theme="@style/ToolbarTheme.BackBar" />
        <activity
            android:name="com.ebook.book.ReadBookActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
                <data android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
                <data android:scheme="file" />
            </intent-filter>
            <!-- EPUB：系统按 MIME 类型分发，content/file 两种 scheme 都需声明 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="application/epub+zip" />
                <data android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="application/epub+zip" />
                <data android:scheme="file" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ebook.book.ImportBookActivity"
            android:theme="@style/ToolbarTheme.BackBar" />
        <activity
            android:name="com.ebook.book.BookDetailActivity"
            android:theme="@style/ToolbarTheme.BackBar" />
        <activity
            android:name="com.ebook.book.DownloadManageActivity"
            android:label="下载管理" />
        <activity
            android:name=".EditBookMetaActivity"
            android:exported="false"
            android:label="编辑匹配信息"
            android:theme="@style/ToolbarTheme.BackBar" />
    </application>
</manifest>