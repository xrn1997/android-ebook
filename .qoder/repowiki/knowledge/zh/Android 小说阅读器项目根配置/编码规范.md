- 所有依赖版本统一通过 `gradle/libs.versions.toml` 版本目录管理，构建脚本中引用 `libs.xxx.yyy`，禁止硬编码版本号。
- 子模块不得在各自 `build.gradle.kts` 中声明仓库，必须通过根 `settings.gradle.kts` 的 `dependencyResolutionManagement` 集中配置，且模式为 `FAIL_ON_PROJECT_REPOS`。
- `gradle.properties` 中的 `isModule` 开关决定模块集成/独立模式，提交态必须保持 `false`，临时调试改回后不得提交该变更。