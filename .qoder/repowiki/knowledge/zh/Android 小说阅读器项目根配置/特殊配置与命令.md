首次 clone 后需执行 `bash scripts/install-hooks.sh` 安装 commit-msg 钩子；构建入口为 `./gradlew :module_app:assembleMockDebug`（内存 mock，无需后端）或 `./gradlew :module_app:assembleRealDebug`（连接 ebook-server，基址经 `local.properties.ebook.server.host` 注入）。Windows PowerShell 下用 `\gradlew`。