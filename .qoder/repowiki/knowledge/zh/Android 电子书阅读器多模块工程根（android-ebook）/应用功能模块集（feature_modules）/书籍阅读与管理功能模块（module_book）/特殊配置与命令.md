独立运行时需将根 `gradle.properties` 的 `isModule` 改为 `true` 后单独编译该模块；修改 TheRouter 注解或新增 `@Route` 后需再构建一次，因为 routeMap 由 transform 回写至 `build/therouter/route`，当次 APK 仍装载旧路由表。集成测试中 Hilt 测试组件需自行声明 `kspAndroidTest(libs.hilt.compiler)` 才能在 `androidTest` 编译期生成注入代码。