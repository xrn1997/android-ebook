Kotlin + Jetpack Compose MVVM（AGP built-in Kotlin，不使用 ViewBinding）；Hilt 2.60.1 + KSP 做依赖注入；TheRouter 1.4.0-rc1 用于跨模块路由与服务发现；Room 3.0.0 + Retrofit/OkHttp 访问数据；Coil 图片加载；协程+Flow 异步；Robolectric + JUnit 单元测试（含 ReaderPagerController 窗口状态机）。