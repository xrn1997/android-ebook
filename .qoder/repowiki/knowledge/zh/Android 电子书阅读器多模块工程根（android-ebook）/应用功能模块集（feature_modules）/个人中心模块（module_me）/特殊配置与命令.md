构建时启用 compose.compiler、ksp 与 xrn1997.hilt 插件；独立运行时把 gradle.properties 的 `isModule` 改为 `true`，并需在 `src/main/test/debug/` 下的 MockHost Activity（如 `LoginActivity.kt`、`MainActivity.kt`）上补路由占位，否则 TheRouter 跳转静默丢失。两个 `AndroidManifest.xml`（集成态 src/main/ 与独立态 src/main/module/）需同步维护 Activity 与权限声明。