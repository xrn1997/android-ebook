`./gradlew :module_app:assembleRealDebug` 构建连真实后端 APK；`./gradlew :module_app:assembleMockDebug` 构建使用 `UserNetworkTest`/`CommentNetworkTest` 内存数据源的 APK（包名 `com.ebook.mock`）。独立调试时将根 `gradle.properties` 的 `isModule=true` 切换后，模块内依赖的功能库被排除，需把业务页面源集挂载到当前 source set。