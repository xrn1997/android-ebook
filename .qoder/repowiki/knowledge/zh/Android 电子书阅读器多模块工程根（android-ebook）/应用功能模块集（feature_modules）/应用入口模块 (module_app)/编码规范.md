- 通过 `network` product flavor 与 source set 双层机制在同一份代码下切换真实后端与 mock 数据源，而非分支开关。
- 应用入口类仅做装配：声明 Hilt 注解、继承共享 `BookApplication`、注册 TheRouter 拦截器，不在 `onCreate` 中放置业务初始化。
- 构建脚本通过 convention alias `libs.plugins.xrn1997.*` 引用约定插件 ID，避免直接硬编码 Gradle plugin version。