独立运行模式需将 gradle.properties 中 `isModule=true` 切换后直接构建运行（默认调试器指向 src/main/module/AndroidManifest.xml 中的 debug 宿主），完成后务必改回 `isModule=false`；TheRouter 新增/改动 @Route 后需再构建一次才能使 routeMap 资产生效，否则该次 APK 仍装载旧路由表。