运行时需要 `local.properties` 中设置 `ebook.server.host=<本机/局域网 IP>`（默认 `10.0.2.2`，模拟器视角的宿主机）；真机联调推荐 `adb reverse tcp:9090 tcp:9090` + `ebook.server.host=127.0.0.1` 以避免 Android 17 本地网络权限限制。集成态通过 module_app 的 `network` flavor 切换 `real`/`mock` 数据源。