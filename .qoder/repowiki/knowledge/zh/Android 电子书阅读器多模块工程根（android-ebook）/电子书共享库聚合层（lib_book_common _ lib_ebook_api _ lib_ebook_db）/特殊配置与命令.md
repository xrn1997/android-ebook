`./gradlew :lib_ebook_db:test` / `:lib_ebook_api:test` / `:lib_book_common:test` 可分别运行各子模块单元测试；集成态构建通过 `./gradlew :module_app:assembleDebug` 一次产出包含三方共享库的完整 APK。开发期后端地址通过根 `local.properties` 的 `ebook.server.host` 注入 `lib_ebook_api.BuildConfig.EBOOK_SERVER_HOST`，无需改动代码即可切换本机/局域网/eureka 服务器。