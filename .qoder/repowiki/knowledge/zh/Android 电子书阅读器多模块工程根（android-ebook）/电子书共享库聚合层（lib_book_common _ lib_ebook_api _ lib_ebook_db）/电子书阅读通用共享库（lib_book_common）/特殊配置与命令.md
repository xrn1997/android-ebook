构建使用仓库根脚本 `./gradlew :lib_book_common:assembleDebug|Release`；测试通过 `./gradlew test`（含 Robolectric 用例）与 `./gradlew connectedAndroidTest`。需确保 `local.properties` 中 `ebook.server.host` 已配置后端地址，否则网络相关断言测试会因目标主机不可达失败。