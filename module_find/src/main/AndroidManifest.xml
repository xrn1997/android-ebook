<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        权限判据与取证见 ADR-0022：本模块只做书城/搜索/书库浏览（网络抓取 + Room），
        不碰 Wi-Fi/定位/电话状态/WakeLock/账号/联系人/相机，也不做文件导入
        （存储权限归 module_book 的导入本地书链路）。
        原先两条 uses-feature camera 未写 required（默认即 true），会把无相机设备挡在
        安装过滤之外，故一并删除——相机特性的显式声明留在 module_app。
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Common">
        <activity android:name=".ChoiceBookActivity" />
        <activity
            android:name=".SearchActivity"
            android:configChanges="locale|keyboardHidden|orientation|screenSize"
            android:theme="@style/CAppTransparentTheme"
            android:windowSoftInputMode="stateHidden|adjustResize" />
    </application>

</manifest>